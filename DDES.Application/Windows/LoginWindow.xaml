<Window x:Class="DDES.Application.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:DDES.Application.ViewModels"
        d:DataContext="{d:DesignInstance vm:LoginViewModel}"
        mc:Ignorable="d"
        Title="Login" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="100px" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50px" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBox Background="#ACAFE1" Grid.ColumnSpan="3"></TextBox>
        <StackPanel Grid.Row="2" Grid.Column="1">
            <TextBlock>Username</TextBlock>
            <TextBox Text="{Binding User.Username}"></TextBox>
            <TextBlock>Password</TextBlock>
            <PasswordBox PasswordChanged="PasswordBox_PasswordChanged"></PasswordBox>
            <Button Command="{Binding LoginCommand}">Login</Button>
        </StackPanel>
    </Grid>
</Window>
